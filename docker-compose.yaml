services:
  api-extractor:
    build: ./system/services/api-extractor
    container_name: api-extractor
    env_file:
      - ./system/.env
    depends_on:
      api-llm:
        condition: service_started
    environment:
      TZ: "America/Sao_Paulo"
    ports:
      - "8001:8001"
    restart: always
    networks:
      - app-network
  
  api-llm:
    build: ./system/services/api-llm
    container_name: api-llm
    env_file:
      - ./system/.env
    depends_on:
      - bd-vetorial
    environment:
      TZ: "America/Sao_Paulo"
    ports:
      - "8002:8002"
    restart: always
    networks:
      - app-network

  api-telegram:
    build: ./system/services/api-telegram
    container_name: api-telegram
    env_file:
      - ./system/.env
    depends_on:
      api-llm:
        condition: service_started
    environment:
      TZ: "America/Sao_Paulo"
    ports:
      - "8004:8004"
    restart: always
    networks:
      - app-network

  api-portal:
    build: ./system/services/api-portal
    container_name: api-portal
    env_file:
      - ./system/.env
    depends_on:
      api-llm:
        condition: service_started
      postgres-arquivos:
        condition: service_healthy
      api-extractor:
        condition: service_started
    environment:
      TZ: "America/Sao_Paulo"
    ports:
      - "8003:8003"
    restart: always
    networks:
      - app-network

  postgres-arquivos:
    image: postgres:17
    container_name: postgres-arquivos
    environment:
      TZ: "America/Sao_Paulo"
      POSTGRES_DB: ${POSTGRES_DB:-agrobot}
      POSTGRES_USER: ${POSTGRES_USER:-agrobot}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-agrobot}
    ports:
      - "5432:5432"
    volumes:
      - postgres_arquivos_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-agrobot} -d ${POSTGRES_DB:-agrobot}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - app-network

  bd-vetorial:
    build: ./system/services/bd-vetorial
    container_name: bd-vetorial
    env_file:
      - ./system/.env
    depends_on:
      qdrant:
        condition: service_started
    environment:
      TZ: "America/Sao_Paulo"
    ports:
      - "8005:8005"
    restart: always
    networks:
      - app-network

  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    environment:
      TZ: "America/Sao_Paulo"
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: always
    networks:
      - app-network

  minio:
    image: minio/minio:RELEASE.2025-02-03T21-03-04Z-cpuv1
    container_name: minio
    environment:
      TZ: "America/Sao_Paulo"
      MINIO_ROOT_USER: admin123
      MINIO_ROOT_PASSWORD: admin123
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    restart: always
    networks:
      - app-network

volumes:
  qdrant_storage:
    driver: local
  minio_data:
    driver: local
  postgres_arquivos_data:
    driver: local

networks:
  app-network:
    driver: bridge
